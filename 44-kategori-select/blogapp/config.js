const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "zeynep123",
        database: "blogdb"
        //blogapp yerine blogdb olarak değiştirdik.
    }
}

module.exports = config;